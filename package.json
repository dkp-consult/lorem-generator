{
  "name": "lorem-generator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm-run-all --parallel live-server:dev watch:dev",
    "build": "npm run copy-files && npm run replace:script-path",
    "live-server:dev": "live-server --port=1511",
    "watch:dev": "watchify src/js/main/app.js -o dist/js/bundle.js -v -d -t babelify",
    "replace:script-path": "replace-in-file 'src=\"js/main/app.js\"' 'src=\"js/bundle.js\"' dist/*.html",
    "copy-files": "cp -r src/styles/* dist/ && src/*.html dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.23.7",
    "@babel/preset-env": "^7.23.7",
    "babelify": "^10.0.0",
    "browserify": "^17.0.0",
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "replace-in-file": "^7.1.0",
    "watchify": "^4.0.0"
  }
}
